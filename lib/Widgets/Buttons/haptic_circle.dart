import 'package:flutter/material.dart';
class HapticCircle extends StatelessWidget {
  const HapticCircle({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}